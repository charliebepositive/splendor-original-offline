# 验证记录

日期：2026-09-05。环境：Windows、Node.js、Chrome。

## 规则与完整对局

执行 `node test/engine.test.cjs`：12 组测试全部通过。

- 90 张卡牌、40/30/20 级别分布、每级每色对称、10 个唯一贵族条件、90 张本地图片。
- 2–4 人初始化、确定性洗牌、普通筹码与黄金供应。
- 取筹码合法性、库存不足、禁止直接拿黄金、错误输入不改变原状态。
- 明牌/暗牌保留、黄金耗尽、手牌上限、市场补牌与空牌堆。
- 折扣与黄金支付、禁止超付、不能买别人的保留牌。
- 超额筹码自主归还、允许归还新获得的黄金。
- 单贵族自动来访、多贵族选一、只看永久折扣。
- 最后一轮公平行动次数、少卡破平、共同获胜。
- 存档校验、AI 不受对手暗牌和牌堆顺序影响。
- 120 局 2–4 人电脑自对弈全部正常结束；每个动作后检查卡牌和筹码守恒、手牌上限及存档有效性。最终版本最长 117 个操作。

## 浏览器操作

使用 Playwright CLI 在真实 Chrome 中操作，购买、热座交接、交接时保留牌隐藏、暗牌保留、超额归还、多贵族选择均通过。桌面 1280×900 与手机 390×844 布局检查无横向溢出。浏览器控制台错误与警告均为 0。

Service Worker 完整缓存 101 项资源（包括所有 90 张卡面和官方 PDF）。关闭网络后重新加载，继续存档成功，市场图片无缺失。

使用 Chrome headless 直接访问本地 `file://.../index.html`，确认不经过 HTTP 服务即可初始化完整开局界面。

浏览器回归脚本与截图保存在工作区上层 `output/playwright`，不包含在游戏压缩包中。单元测试及规则源文件包含在压缩包中。
